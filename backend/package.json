{
  "name": "truckaccounting-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "TruckAccounting backend — REST API over MySQL/MariaDB. See README.md.",
  "engines": {
    "node": ">=22.6.0"
  },
  "scripts": {
    "start": "node src/index.ts",
    "dev": "node --watch src/index.ts",
    "test": "node --test test/*.test.ts",
    "migrate": "node scripts/migrate.ts",
    "create-admin": "node scripts/create-admin.ts"
  },
  "dependencies": {
    "mysql2": "^3.24.2"
  }
}
