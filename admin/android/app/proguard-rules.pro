# Add project-specific ProGuard rules here as they become necessary once the release build is
# actually exercised. Kept intentionally minimal: Hilt and Retrofit/Gson each already publish
# consumer-rules via their AARs, so no manual keep rules should be needed for what this app uses.
