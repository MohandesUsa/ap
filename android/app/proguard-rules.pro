# Add project-specific ProGuard rules here as they become necessary once the release build
# is actually exercised (Phase 2 only ships a debug build — project rule §26).
# Kept intentionally minimal: Hilt, Room, and Retrofit/Gson each already publish consumer-rules
# via their AARs, so no manual keep rules should be needed for the libraries used in Phase 2.
